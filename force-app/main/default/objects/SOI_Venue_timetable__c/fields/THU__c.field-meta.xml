<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>THU__c</fullName>
    <externalId>false</externalId>
    <formula>TEXT(HOUR( ThuStart__c ))+ &#39;:&#39; + IF(MINUTE( ThuStart__c )&lt;10, &#39;0&#39; + TEXT(MINUTE( ThuStart__c )), TEXT(MINUTE( ThuStart__c )))
+ &#39;-&#39; +
TEXT(HOUR( ThuEnd__c )) + &#39;:&#39; + IF(MINUTE( ThuEnd__c )&lt;10, &#39;0&#39; + TEXT(MINUTE( ThuEnd__c )), TEXT(MINUTE( ThuEnd__c )))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>THU</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
