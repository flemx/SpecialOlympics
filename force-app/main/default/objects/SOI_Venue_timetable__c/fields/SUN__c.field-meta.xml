<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>SUN__c</fullName>
    <externalId>false</externalId>
    <formula>TEXT(HOUR( sunStart__c ))+ &#39;:&#39; + IF(MINUTE( sunStart__c )&lt;10, &#39;0&#39; + TEXT(MINUTE( sunStart__c )), TEXT(MINUTE( sunStart__c )))
+ &#39;-&#39; +
TEXT(HOUR( sunEnd__c )) + &#39;:&#39; + IF(MINUTE( sunEnd__c )&lt;10, &#39;0&#39; + TEXT(MINUTE( sunEnd__c )), TEXT(MINUTE( sunEnd__c )))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>SUN</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
