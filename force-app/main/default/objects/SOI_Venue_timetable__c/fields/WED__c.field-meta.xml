<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>WED__c</fullName>
    <externalId>false</externalId>
    <formula>TEXT(HOUR( wedStart__c ))+ &#39;:&#39; + IF(MINUTE( wedStart__c )&lt;10, &#39;0&#39; + TEXT(MINUTE( wedStart__c )), TEXT(MINUTE( wedStart__c )))
+ &#39;-&#39; +
TEXT(HOUR( wedEnd__c )) + &#39;:&#39; + IF(MINUTE( wedEnd__c )&lt;10, &#39;0&#39; + TEXT(MINUTE( wedEnd__c )), TEXT(MINUTE( wedEnd__c )))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>WED</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
