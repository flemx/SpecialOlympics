<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>TUE__c</fullName>
    <externalId>false</externalId>
    <formula>TEXT(HOUR( monStart__c ))+ &#39;:&#39; + IF(MINUTE( monStart__c )&lt;10, &#39;0&#39; + TEXT(MINUTE( monStart__c )), TEXT(MINUTE( monStart__c )))
+ &#39;-&#39; +
TEXT(HOUR( monEnd__c )) + &#39;:&#39; + IF(MINUTE( monEnd__c )&lt;10, &#39;0&#39; + TEXT(MINUTE( monEnd__c )), TEXT(MINUTE( monEnd__c )))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>TUE</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
