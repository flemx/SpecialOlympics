<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>SAT__c</fullName>
    <externalId>false</externalId>
    <formula>TEXT(HOUR( satStart__c ))+ &#39;:&#39; + IF(MINUTE( satStart__c )&lt;10, &#39;0&#39; + TEXT(MINUTE( satStart__c )), TEXT(MINUTE( satStart__c )))
+ &#39;-&#39; +
TEXT(HOUR( satEnd__c )) + &#39;:&#39; + IF(MINUTE( satEnd__c )&lt;10, &#39;0&#39; + TEXT(MINUTE( satEnd__c )), TEXT(MINUTE( satEnd__c )))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>SAT</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
